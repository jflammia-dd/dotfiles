interface:
  display_name: "MCP OAuth Repair"
  short_description: "Repair OAuth credentials across agent runtimes"
  default_prompt: "Use $mcp-repair to reconnect failed OAuth-backed MCP servers."
policy:
  allow_implicit_invocation: true
